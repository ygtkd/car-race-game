# 改修5 実装・検証記録

対象: COAST_RACER_REVISION_PROMPT_5.md。実装・push・Azure公開まで依頼済み。既存CarController.csは対象外。

| 要件 | 修正・確認内容 |
| --- | --- |
| ゴールテープ | 道路装飾の重なり除外がFinish chequerにも適用されていた。明示的に除外対象から外し、灰色路肩まで線を延長。頭上の市松模様テープと両端支柱を追加 |
| 周回 | 終点判定の道路幅限定を灰色路肩まで拡大。芝生・逆走・高さ違い・順序抜けは拒否。中間ゲートには走行距離の前後による通過判定を追加 |
| 地形 | 路面リボンと起伏地形を独立法線の両面メッシュ化。最下層に不透明な地面を追加。前回の上下道路の分離を維持 |
| スペシャル | 発動増減量を2倍、全車5秒。パルス半径24m・減速36%・継続減速4・妨害5秒。加速・最高速・グリップ・接触重量の増加分を2倍。完全免疫はそのまま。パルスの1秒後描画終了をなくし、発動中繰り返し描画 |
| 名前 | 名前背景・枠線を除去し、文字と影を維持 |
| 後方視点 | アクセル上のボタンで三人称後方視点を切替。再タップ・停止・画面遷移・フォーカス喪失で解除。二本目の指でもタッチ開始を処理し、アクセル長押しを維持 |

## 進捗
- 2026-09-24再開時の空き容量10.8GiB。新規インストールなし。
- 共通走行の既存32レース・128台分、改修4、改修5の追加テスト成功（Logs/revision5-core-final.log）。灰色での2周目ゴール・逆走拒否、全8車種の5秒持続と終了、加速増加量・パルス範囲と減速率を確認。
- 最初のパルス試験で減速係数の置換漏れを検出し修正。再実行で成功。
- WebGLビルド中。裏面の法線改善と二本指ミラー操作の変更を含めて再ビルドが必要。
- オンライン鈴鹿2周走行を検証中。ブラウザーでの後方視点、名前、終盤エフェクト、上下道路の画像確認はこれから。
- push・Azure公開は未実施。実機iPhone/Galaxy/Pixelおよび8人の本番継続負荷は未確認。
- 初回の版6ce61598c643で追加ブラウザー35項目・既存回帰118項目成功、エラー0。実タッチの二本目で後方視点が切り替わり、アクセル入力が維持されることを確認。名前背景透明、停止時カメラ復帰、全8車種の効果残り1秒未満での表示を確認。
- 立体交差の上下10地点を撮影。上側道路の路肩外に広い芝生面の重なりが残ることを画像で検出したため、別区間が近い部分は高さに関係なく広い芝生リボンを省き、両面の起伏地形と最下層の地面で覆う修正を追加。最終版を再ビルド・再撮影する。
- オンライン鈴鹿2周完走、両クライアントの結果一致、再戦成功（Logs/revision5-online.log）。ソロは座標・周回数を直接変更せず、入力だけで2周実走行を検証中。
- ソロ鈴鹿も入力による実走行で2周完走・記録保存・バッジ表示・メニュー復帰まで成功、実行時エラー0（Logs/revision5-solo-drive.log、revision5-special-drive.json）。走行で得たゲージによる発動、停止中の残時間保持、再開・終了・再充填も成功。位置・周回数・ゲートの直接書き換えはしていない。
- 地形の再確認で、灰色の路肩が道路の下にも全面で敷かれ、曲がった傾斜面で交差し得る構造を確認。路肩と芝生を左右の帯に分割し、路面との重複をなくす追加修正を実施。基礎地面も起伏地形の最低点より下へ移した。最終版を再ビルド中。
- 52b86da8ea36でのエフェクト再試験は、残り0.75秒の画像取得後にHUDがまだ有効であることを検査していたため、撮影時間によって正しく終了した効果を失敗扱いした。視覚用スナップショットを撮影中に保持するテストへ修正。5秒の時間計測は共通ロジックと実走行の別テストで確認済み。

## 最終ローカル検証完了（2026-09-24）
- 最終WebGL Build: 5eb6ff04bcd7。Logs/revision5-unity-release.logにビルド成功記録。
- 路肩と芝生を左右の帯にした最終画像で、鈴鹿の右側道路に出ていた縞状の重なりが消えていることを確認。上下・進入・退出10地点の高さと区間確認に成功（Logs/revision5-release-scenery.log、revision5-crossing-*.png）。
- 最終版の追加ブラウザー35項目成功・エラー0（Logs/revision5-special-checks.json、revision5-release-browser.log）。二本目の指での後方切替、長押しアクセル維持、前方復帰、停止解除、名前背景透明、全車の終盤エフェクト、8台同時表示、終了後の消去を確認。
- 最終版の版別パッケージ起動8項目成功・エラー0（Logs/revision5-release-checks.json）。全スクリプトの同一版、更新案内、鈴鹿レース開始を確認。
- 共通走行・ソロ2周・オンライン2周・既存回帰118項目は、同じ走行ロジックとUIで成功済み。最後の追加変更は地形メッシュの配置だけで、上記の画像・起動確認を再実行した。
- WebBuildへ最終版を反映。新規インストールなし。実機iPhone/Galaxy/Pixel、ホーム画面起動、8人の本番継続負荷は未確認。

## Azure公開完了（2026-09-24）
- 実装コミット89ac0c52ef2748e280762f29aeebd38a2e011a1eをmainへpush済み。
- Actions https://github.com/ygtkd/car-race-game/actions/runs/35993094386 はsuccess。無料枠確認・デプロイ・公開アプリ確認の全ステップ成功。
- 公開URL: https://car-race-game-gmczdrgba5bph0gp.japanwest-01.azurewebsites.net/play/ 。公開Build 5eb6ff04bcd7、health=ok（protocol 3）。
- 公開ブラウザー11項目成功、実行時エラー0（Logs/revision5-public-checks.json）。版一致、更新通知、鈴鹿開始、後方視点切替と前方復帰、名前背景削除を確認。
- 記録API /api/records/ridge は初回応答エラー、再試行でHTTP 200。初回失敗原因までは特定していない。
- 新規インストールなし。空き容量約10.2GiB。実機iPhone/Galaxy/Pixel、ホーム画面起動、8人の本番継続負荷は未確認。
- CarController.csのユーザー既存変更は変更・コミットせず保持。
