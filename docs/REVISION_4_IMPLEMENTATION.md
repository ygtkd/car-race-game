# 改修プロンプト4 実装・検証台帳

対象: COAST_RACER_REVISION_PROMPT_4.md。ユーザーは実装・push・Azure公開を依頼。今回の実機報告のBuildは未確認、調査元の公開Buildは8d6b44e43f67。

| 要件 | 調査・修正 | 証拠と限界 |
| --- | --- | --- |
| R4-1 鈴鹿の立体交差 | 元の広い芝生リボンが上層の高さで下層の道路まで覆う構造。別区間の下道がある部分では上層の広い地表リボンを生成せず、道路と灰色路肩の下に橋床を生成。観客席等は高さに関係なく全道路区間の平面範囲と照合し、干渉する配置を除外。木にも同じ検査 | 上下と前後10地点のブラウザー画像と高さ・区間検証。Logs/revision4-crossing-*.png、revision4-crossing-checks.json。実機の見え方は未確認 |
| R4-2 周回 | 元の中間ゲートは道路内だけに限定し、路肩通過で順番が止まる。全中間ゲートを道路周辺の走行可能領域まで広げ、急カーブの内外差を吸収。順番・距離・高さ・前進条件は維持。最終ラインは実際の横断判定であり、重複の道路幅条件と1m/s最低速度条件を除去。近接道路への飛び移りを抑えるため物理更新での最近傍検索を前後6サンプルに制限 | 両コース全23ゲートの左右灰色ゾーン、低速ゴール、二重計上・ショートカット拒否、上下の区間維持を追加テスト。ブラウザー実走行と実通信の結果は下へ追記 |
| R4-3 アクセル残留・操舵不能 | タッチではTouch Eventsの現在の全指一覧に保持状態を照合し、終了通知の取りこぼし後も古いIDを排除。マウス／ペンはPointer Eventsを使い、入力源を二重登録しない。判定外への移動、キャンセル、画面離脱・回転で解除。Unityにも0.75秒の入力更新期限を追加。ブラウザー送信ループが0.75秒以上止まった場合も保持状態を解除。サーバー既存の750ms期限は維持 | 部分的な指離し、古いID、連打30回、停止復帰10回など。PC Edgeのタッチエミュレーションであり、実機での報告原因を断定しない |
| R4-4 数字70％ | 速度23→16.1px、小画面20→14px、ギア13→9.1px。直径・km/h・他のHUDは維持 | 高さ650/393pxで計算済みスタイルを確認 |

## テスト方針
- 共通走行の既存32レース128台分と追加のR4チェックが成功（Logs/revision4-core.log）。
- オンラインの最初の実走行は両者が同一更新内で2周完走。テストが「必ず一人DNF」と固定していたため失敗扱いになった。ゲーム側は両者の実測タイムを保持しており、既存の同時完走仕様に沿う。テストを、完走タイムありまたは未完走最下位のタイムなしを許容し、両クライアントの順位・タイム一致を確認する形へ修正した。
- タッチの最初の部分解除テストはCDPのtouchEndに残す指を指定していた。実際には指定した指を終了させるため、テストを訂正。イベントログで、アクセルと操舵のどちらが終了したかを照合した。
- 描画の10地点はテスト用位置スナップショット。2周の実走行検証とは区別する。
- 新規インストール・外部素材なし。CarController.csのユーザー既存変更は触れない。
- 実機iPhone/Galaxy/Pixel、ホーム画面起動、8人の本番継続負荷は未確認。

## ビルド
Unity 6000.6.2f1の最終ビルド成功。版別パッケージc99d23daba52をWebBuildへ反映。公開結果は検証後に追記する。
## 再開後の検証
- 最終WebGLビルド成功（Logs/revision4-unity-final.log）。版別パッケージはc99d23daba52。
- 立体交差の上下・進入・退出10地点の画像を確認。道路の連続性と走行する高さを確認し、道路上に重なっていた観客席を除外（Logs/revision4-crossing-checks.json）。
- 操作77項目成功、実行時エラー0。速度・ギアの70％、ペダルだけ離す／操舵だけ離す／キャンセル／古い指IDの排除／連打30回／停止復帰10回／復帰後のゲージ増加を確認（Logs/revision4-browser-checks.json）。
- 鈴鹿の実通信2周完走、両者の結果一致、再戦まで成功（Logs/revision4-full-online-final.log）。今回の最終テストでは未完走最下位のタイムなしも確認。前回は同時完走の両者タイム保持を確認。
- 既存回帰の並行実行ではオンライン設定開閉10回まで通過したが、スクリーンショット取得でCDPがタイムアウト。負荷の高い並行実行を避けて再実行する。失敗を実機・ゲーム動作の合格証拠には扱わない。
- 鈴鹿ソロもブラウザー内の入力だけで2周完走し、記録保存・バッジ表示・メニュー復帰まで成功（Logs/revision4-suzuka-drive.log、revision4-suzuka-drive.json、エラー0）。急カーブでテスト用自動操舵が何度か旋回したため走行時間は長いが、座標・周回数・チェックポイントの強制変更は行っていない。
- Deployment/Prepare-CI.ps1による公開ZIP作成成功。ソースと梱包したJSの差分は末尾改行のみで、実装内容は一致。
- 既存回帰の単独再実行は118項目すべて成功、実行時エラー0（Logs/revision4-smoke-final.log、Logs/club-browser-checks.json）。通信異常時の切断・古い接続の無効化・再接続も成功。
- 版別パッケージのブラウザー起動8項目成功、実行時エラー0（Logs/revision4-release-checks.json）。Build c99d23daba52、全スクリプト同一版、更新通知、鈴鹿レース開始、スタート文字非表示を確認。
