# 改修プロンプト2 実装・検証台帳

対象: docs/COAST_RACER_REVISION_PROMPT_2.md。今回はローカル実装と検証。push・Azure公開は未実施。

| 要件 | 実装変更 | 確認方法・未確認事項 |
| --- | --- | --- |
| P0-1 版混在 | 設定のビルドID、release.json、版別ディレクトリでJS/CSS/音声/Unityを統一。メニューでのみ更新操作可能 | パッケージとブラウザー検証。ホーム画面実機は未確認 |
| P0-2 開始文字 | centerMessageの非表示をCSSで固定。信号と音のみ維持 | レース開始と旧文字を入れた回帰テスト。実機の報告は再確認待ち |
| P0-3 芝生警告 | 描画縁石と判定でGrassEdge共用。onGrassで表示を制限し、非芝生で即時リセット | 両コースの芝生→縁石境界ロジックとブラウザー表示テスト |
| P0-4 復帰 | 保存した座標・区間・向きを直接使用 | 復帰直前のindexを離しても保存位置・向きに戻る回帰テスト |
| P1-1 右上 | 前回修正を維持 | 3画面幅のチェックを継続。ホーム画面セーフエリア未確認 |
| P1-2 ゴール | 完走した車両の補間遅延を止め物理位置を即反映 | 共通ロジックのライン通過検証。実機の見え方は未確認 |
| P1-3 結果 | CPU推定の距離を次チェックポイントまでに制限。90秒・900秒終了を削除。lobbyの古い状態を結果として処理しない | 両コースの実通信完走・再戦。人間の推定禁止は既存テスト維持 |
| P1-4 ロビー | 入力中のCPU数を定期通知で上書きしない | 既存のホスト権限・8台開始テストを維持 |
| P1-5 通信 | 無応答検知も異常切断から再接続へ進む | 異常後の復旧テスト。実機ネットワーク切替は未確認 |
| P1-6 バッジ | 種類別SVG画像、レースIDごとの永続化・表示済み削除 | 人工データによる表示テストと実走行を区別。ホーム画面実機の獲得表示は未確認 |
| P2 遠景 | 鈴鹿の周辺施設・観覧車を追加、既存背景と雲を維持 | ブラウザー画像で確認。実機画質・継続性能は未確認 |

切断離脱は20秒猶予後にDNFとし、復旧不能の車を待ち続けない。残る走行車が1台の場合はタイムなしで確定する既存ルールを維持。人間タイムは推定しない。明示退室は再接続対象外。

既存機能は共通走行テスト、ブラウザーSmoke-Club、通信Test-Race-Flowで回帰確認する。テスト件数だけを全要件達成と扱わない。

## 未確認・完了扱いにしない項目
- iPhone 17、Galaxy、Pixelの実機（特にホーム画面起動）と音の聴感。
- Azure無料枠上での8人継続負荷。今回は公開指示ではないため本番負荷試験は行わない。
- 古いホーム画面アプリで現在表示されているコードの版は端末側情報がないため未確定。更新パッケージ導入後にビルドIDで照合可能。
- 新規インストールなし。外部素材なし。SVGはコードで作成。

## 今回の検証結果（2026-09-23）
- 最終WebGLビルドID: `65bd238ef35d`。Unity 6000.6.2f1でビルド成功。WebBuildへ反映済み。push・Azure公開は未実施。
- 共通走行ロジック: 両コースのライン手前／前端通過／逆走／立体交差、芝生→縁石で警告解除、保存区間への復帰、人間タイムを推定しないことを確認。32レース・128台分の完走と8台走行を確認（Logs/revision2-core-final.log）。
- ブラウザー: 88項目、実行時エラー0（Logs/club-browser-checks.json）。この検証時の版は55308688859f。その後の変更は鈴鹿背景の配置調整で、最終版では別途8項目の版統一・実レース開始検証に合格（Logs/revision2-release-checks.json）。
- 実通信: ローカルの8人接続、ホストのCPU指定、放置切断など13項目に合格（Logs/revision2-rooms.log）。両コースで2接続の完走、最下位の記録なし、結果一致と再戦を確認（Logs/revision2-fullrace.log）。本番8人の継続負荷とは区別する。
- 最終版の鈴鹿レース画像で、周辺施設が道路を遮らず、背景・雲・右上設定ボタン・信号が表示されることを確認（Logs/revision2-final-suzuka.png）。写実的な質感や実機性能を保証する証拠ではない。
- Deployment/Prepare-CI.ps1によるローカル公開パッケージ作成に成功（Logs/revision2-package-check.log）。

## 既存要望の回帰確認の対応
| 要望 | 確認した証拠と範囲 |
| --- | --- |
| 日本語、端末プロフィール、名前、言語、独立音量 | Smoke-Club: 初期日本語、再読み込み後の名前・音量・言語の保持。ログイン不要 |
| 図鑑・360度・装飾・記録・8車種 | Smoke-Club: 8車種選択、新車種プレビュー、図鑑ドラッグ回転、バッジ装備・記録保持。特殊技と走行は共通走行テスト |
| 開始前コース非表示・画面遷移 | Smoke-Club: メニュー・コース選択・オンラインロビーで非表示、開始時ロード表示 |
| 音楽・SE・エンジン | Smoke-Club: 音声デコード、AudioContext、ミュートと音量保持。曲調・エンジン音の実機聴感は未確認 |
| 操作と画面幅 | Smoke-Club: 852/915/900幅で右上の枠なし歯車と幅2倍の長方形操舵、タッチ操舵・ペダル・キャンセルによる解除。長押し選択／拡大抑止CSSも確認。物理端末の複数指操作は未確認 |
| 2周、共有コイン、衝突 | 共通走行／通信テストを維持。実端末間の同期表示と長時間負荷は未確認 |
| 名称・アイコン・戻る・設定・一時停止 | Smoke-Club: COAST RACER、角の戻る、設定の×・背景タップ、一時停止・退出確認。ホーム画面アイコン起動は未確認 |
| アシスト、勾配、バランス、名前表示 | 共通走行: 全車種・両コース・開始位置を変えた完走。Smoke-Club: 保存名と車上ラベル。対人戦での体感バランスは未確認 |

## 実機での再確認手順（公開後）
1. ホーム画面から起動してゲーム設定を開き、Buildが `65bd238ef35d` であることを確認する。異なる場合は更新または再読み込み後に再確認する。
2. ソロの初回と再戦、オンラインの開始で、信号のみが表示され、「3・2・1」「スタート」が重ならないことを確認する。
3. 道路・縁石では復帰警告なし、芝生でのみ残り5秒からの表示、道路へ戻ると即解除、時間切れでは最後の道路中央へ戻ることを確認する。
4. OS・ブラウザー・起動方法・Buildを添えて結果を記録する。実機未確認のP0-1〜P0-4、P1-1〜P1-2、P1-5〜P1-6、P2、およびAzure負荷試験は完了扱いにしない。

## 実走行からのバッジ検証
最終版65bd238ef35dをEdgeで実行し、操舵・アクセル入力による山岳コース2周完走（2分19秒85）、記録保存、初完走バッジ画像の表示、閉じてメニューへ戻るまで成功。結果データの注入は行っていない。画像取得は非同期のため、テストを画像読込完了待ちに修正した。ブラウザー実行時エラー0。証拠: Logs/revision2-real-finish.json、Logs/revision2-real-finish-badges.png。物理端末の確認とは区別する。
